version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: anisgolang-app
    container_name: anis-app
    restart: unless-stopped
    ports:
      - "8010:8011"
    networks:
      - anis

networks:
  anis:
    driver: bridge

volumes:
  db_data:
    driver: local
